import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Profile({}: Props): ReactElement {
    return (
        <div className="App">
            
        </div>
    )
}
